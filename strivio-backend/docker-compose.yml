services:
  strivio-db:
    image: postgis/postgis:15-3.3
    container_name: strivio_spicalabs_db
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - strivio_pgdata:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  strivio_pgdata: